admin:
  existingSecret: grafana-password
  userKey: admin
  passwordKey: password
ingress:
  enabled: true
  hostname: grafana.wordpress.jyeclc.com
  path: /
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-staging
  tls: true