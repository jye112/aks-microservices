apiVersion: spv.no/v1alpha1
kind: AzureKeyVaultSecret
metadata:
  name: password
spec:
  vault:
    name: jyeakskeyvault01
    object:
      name: db-secret
      type: secret
  output:
    secret: 
      name: password
      dataKey: password
      type: opaque