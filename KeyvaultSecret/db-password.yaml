apiVersion: spv.no/v1alpha1
kind: AzureKeyVaultSecret
metadata:
  name: db-password
spec:
  vault:
    name: jyeakskeyvault01
    object:
      name: db-secret
      type: secret
  output:
    secret: 
      name: db-password
      dataKey: password
      type: opaque